machine:
  node:
    version: 7.7.1
dependencies:
  override:
    - npm install -g npm@next
    - npm install -g bower
    - npm install
    - npm install --only=dev
general:
  artifacts:
    - ./coverage
